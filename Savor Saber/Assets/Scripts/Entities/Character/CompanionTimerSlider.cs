using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;
public class CompanionTimerSlider : MonoBehaviour
{

    public Slider slider;
    //exists purely to pass on a reference to the timer slider since getcomponentinchildren may not find the right one

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

}
